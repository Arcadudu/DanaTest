<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.arcadudu.danatest">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen"
            android:label="Home" />
        <activity android:name=".test.test3_four_options.Test3FourOptions" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".test.test1_enter_word.Test1EnterWord" />
        <activity android:name=".topic_selector.TopicPickerActivity" />
        <activity android:name=".main_selector.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>